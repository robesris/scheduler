<h1>Editing faction</h1>

<% form_for(@faction) do |f| %>
  <%= f.error_messages %>

  <p>
    <%= f.label :name %><br />
    <%= f.text_field :name %>
  </p>
  <p>
    <%= f.submit 'Update' %>
  </p>
<% end %>

<%= link_to 'Show', @faction %> |
<%= link_to 'Back', factions_path %>